module WikisHelper
=begin
    def user_is_authorized_to_edit?
        current_user &&
             (current_user == @wiki.user)
    #        || current_user.admin?
    #        || current_user.collaborator?
            #|| (@wiki.user == nil)

    end
=end
end
